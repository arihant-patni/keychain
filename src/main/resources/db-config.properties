# Database connection properties (used by DataSourceConfig)
# Use an external override at runtime if needed (see README/GRADLE docs)

# Example MySQL configuration (replace values for your environment)
# For local development you can run a MySQL instance on port 3306 and update credentials below
db.url=jdbc:mysql://localhost:3306/keychain?useSSL=false&serverTimezone=UTC
db.username=root
db.password=secret
db.driverClassName=com.mysql.cj.jdbc.Driver
# Hikari pool size
db.maximumPoolSize=10


